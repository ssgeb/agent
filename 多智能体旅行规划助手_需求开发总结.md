# 多智能体旅行规划助手：需求开发总结

## 1. 项目定位

本项目是一个基于大模型与多智能体协同的旅行规划系统，面向用户自然语言旅行需求，支持通过多轮对话完成需求理解、任务拆解、信息检索、方案生成与持续调整，最终输出可执行的旅行方案。

项目当前确定的核心架构为：

- 1 个主 Agent：Planner / 主助手 Agent
- 3 个子 Agent：
  - Transport Agent（航班 / 交通 Agent）
  - Hotel Agent（酒店 Agent）
  - Itinerary Agent（行程 / 景点 Agent）

其中天气、地图、搜索等能力通过 tool 或 MCP 服务接入，不单独作为 Agent。

---

## 2. 项目目标

构建一个可扩展、可迭代、可工程化落地的多智能体旅行规划助手，具备以下核心能力：

- 支持自然语言输入旅行需求
- 支持多轮对话与状态记忆
- 能自动拆解交通、酒店、行程等任务
- 能调用工具或 MCP 服务获取外部信息
- 防prompt提示词注入
- 能生成结构化、可调整的旅行方案
- 能作为 FastAPI 后端服务独立运行
- 第一版先以 mock tools 实现最小可运行版本

---

## 3. Agent 架构设计

## 3.1 主 Agent

### Planner / 主助手 Agent
职责包括：

- 理解用户旅行需求
- 提取结构化字段
- 识别当前问题所属模块
- 拆解任务并路由到子 Agent
- 汇总子 Agent 返回结果
- 负责追问、澄清与重规划
- 统一维护会话状态与 trip_state

---

## 3.2 子 Agent

### 1. Transport Agent
负责：

- 识别交通相关需求
- 查询航班 / 高铁 / 火车候选方案
- 根据价格、时长、时间段等条件做筛选
- 给出交通推荐及理由
- 响应用户对交通方案的修改需求

### 2. Hotel Agent
负责：

- 识别住宿需求
- 查询酒店候选项
- 根据预算、地段、评分、交通便利度等条件筛选
- 输出推荐酒店及理由
- 响应用户对酒店方案的替换和调整

### 3. Itinerary Agent
负责：

- 检索景点、攻略、玩法信息
- 理解用户兴趣与旅行节奏偏好
- 结合旅行天数生成每日 itinerary
- 使用天气、地图、RAG 等工具优化行程顺序
- 响应用户对行程安排的增删改需求

---

## 4. 工具与 MCP 服务设计

以下能力不作为独立 Agent，而是作为子 Agent 可调用的外部能力：

- 天气查询工具
- 地图 / 路线工具
- 搜索工具
- 航班 / 高铁 / 火车查询工具
- 酒店查询工具
- RAG 检索工具
- Agent-Reach 外部信息检索工具
- 其他后续接入的 MCP 服务

设计原则：

- Agent 负责决策
- Tool / MCP 负责提供外部数据或执行外部能力
- 各子 Agent 内部按需调用工具，而不是继续拆出更多业务 Agent

## 4.1 Agent-Reach 接入定位

本项目可以借助 Agent-Reach 项目的搜索、网页读取、社交平台读取、视频字幕提取、GitHub / RSS / 公众号读取等能力，作为旅行规划助手的外部信息检索补充层。

Agent-Reach 在本项目中的定位不是新的业务 Agent，而是 tool / MCP 能力来源或适配层：

- 为 Itinerary Agent 提供景点攻略、玩法文章、本地经验、公开视频字幕、RSS / 公众号内容等检索来源
- 为 Hotel Agent 补充酒店周边、商圈、用户口碑、交通便利度等非结构化信息
- 为 Transport Agent 补充交通政策、出行提示、机场 / 车站信息、临时变更公告等公开信息
- 为 Planner Agent 提供综合检索能力，用于发现用户需求中的缺失信息、冲突信息和需要追问的点

建议在工程实现上新增统一的 `ExternalSearchTool` 或 `AgentReachSearchTool` 抽象，屏蔽 Agent-Reach 底层命令差异。子 Agent 只依赖统一工具接口，不直接耦合具体命令。

可优先接入的 Agent-Reach 能力包括：

- 网页阅读：使用 Jina Reader 读取旅游攻略、景点说明、官方页面等网页内容
- 全网搜索：使用 Exa / MCP 搜索目的地、景点、攻略、交通信息
- 视频字幕：使用 yt-dlp 获取 YouTube / B站旅游视频字幕，用于总结玩法和注意事项
- RSS / 公众号：读取旅游攻略、城市活动、目的地资讯等更新内容
- 社交平台：后续可按需接入小红书、微博、V2EX、Reddit 等渠道，补充用户口碑与本地经验

第一版仍建议先做 mock 检索工具和基础 RAG；Agent-Reach 可以作为后续替换真实检索能力的优先方案。

---

## 5. 用户侧功能需求

## 5.1 旅行需求输入
用户可以通过自然语言输入旅行需求，系统应支持识别：

- 出发地
- 目的地
- 出发日期
- 返回日期
- 出行天数
- 出行人数
- 预算范围
- 交通偏好
- 酒店偏好
- 景点偏好
- 旅行节奏偏好
- 特殊要求与限制条件

支持方式：

- 一次性完整输入
- 多轮逐步补充
- 中途修改条件
- 系统主动追问缺失信息

## 5.2 多轮对话
系统应支持：

- 继承当前会话上下文
- 记住已经确认的旅行条件
- 对局部模块继续追问
- 对已有方案做局部修改或整体重规划

## 5.3 旅行方案生成
系统应生成包含以下模块的方案：

- 交通推荐
- 酒店推荐
- 每日行程安排
- 景点与玩法说明
- 预算估算
- 天气或出行注意事项

## 5.4 方案调整
支持用户进一步提出修改，例如：

- 更改预算
- 更改交通方式
- 更换酒店
- 调整行程节奏
- 增加或删除景点
- 修改某一天的安排
- 重新生成整体方案

---

## 6. 主 Agent 功能需求

Planner Agent 需要具备以下能力：

- 解析用户输入并提取结构化字段
- 判断当前输入属于交通、酒店、行程或综合规划需求
- 根据需求决定调用哪些子 Agent
- 在信息不充分时进行追问
- 汇总多个子 Agent 返回结果
- 发现预算、位置、时间上的冲突并提示
- 统一输出自然语言答复
- 维护 conversation state 与 trip_state

---

## 7. 子 Agent 功能需求

## 7.1 Transport Agent 功能
- 判断用户更适合飞机、高铁或其他交通方式
- 查询交通候选方案
- 支持价格、时间、总耗时、时段偏好等筛选
- 输出多个候选方案和推荐理由
- 支持根据用户反馈重新筛选或替换方案

## 7.2 Hotel Agent 功能
- 识别住宿预算和偏好
- 查询酒店候选数据
- 基于评分、价格、位置、性价比做排序
- 输出多个候选酒店和推荐原因
- 支持对酒店方案做局部替换

## 7.3 Itinerary Agent 功能
- 检索景点与玩法内容
- 根据用户兴趣生成每日行程
- 考虑通勤时间、开放时间、天气与节奏合理性
- 提供景点替换、顺序重排、节奏调整能力
- 让行程具备较好的可执行性与可读性

---

## 8. 状态管理需求

状态系统是整个项目的核心之一。

## 8.1 ConversationState
用于记录对话级上下文，建议包含：

- session_id
- message_history
- summary
- current_intent
- active_agent
- pending_questions
- tool_results
- last_plan
- final_response

## 8.2 TripState
用于记录结构化旅行信息，建议包含：

- origin
- destination
- start_date
- end_date
- duration_days
- budget
- travelers_count
- traveler_type
- transport_preferences
- hotel_preferences
- attraction_preferences
- pace_preference
- must_visit_places
- excluded_places
- notes

## 8.3 状态更新要求
系统应支持：

- 自动从用户输入中提取并更新状态
- 用户修正信息后覆盖旧字段
- 各子 Agent 共享读取 trip_state
- Planner 统一写回状态
- 长对话下通过 summary 压缩上下文

---

## 9. 输出方案需求

最终方案建议采用结构化输出，包括：

- 方案概览
- 交通推荐
- 酒店推荐
- 每日行程
- 预算估算
- 注意事项

可支持不同粒度：

- 简版：快速概览
- 标准版：完整行程与推荐理由
- 详细版：更细的时间安排与候选项说明

---

## 10. 后端与工程需求

## 10.1 API 设计
第一版建议至少提供：

- `/health`：健康检查
- `/chat`：主对话接口
- `/session/{id}`：获取会话状态
- `/plan/{id}`：获取当前方案

## 10.2 服务端能力
- 会话隔离
- 会话恢复
- 日志记录
- 配置管理
- 基础错误处理
- 结果持久化或缓存

## 10.3 推荐技术栈
- FastAPI
- LangGraph
- LangChain
- Pydantic
- Python 异步编程
- 后续可扩展 Redis / MySQL / MCP 服务

---

## 11. RAG 相关需求

RAG 主要服务于 Itinerary Agent，而不是单独形成新 Agent。

主要功能包括：

- 导入旅游攻略内容
- 对攻略进行分块与索引
- 根据目的地和兴趣做检索
- 返回 top-k 内容给行程规划使用
- 对检索结果做基础去重与清洗
- 增强景点推荐与本地玩法建议
- 支持将 Agent-Reach 获取的网页、攻略、公众号、RSS、视频字幕等内容作为 RAG 数据来源

第一版可以做基础 RAG，不必一开始就做复杂 rerank 和多路召回。

建议数据流为：

1. Agent-Reach 负责搜索与读取外部内容
2. 内容清洗模块负责去噪、截断、结构化与来源记录
3. RAG 模块负责分块、索引、召回与去重
4. Itinerary Agent 基于检索结果生成或调整行程

---

## 12. 安全与异常处理需求

系统应具备基础安全与异常兜底能力：

- 输入参数校验
- 工具调用白名单
- 超时与失败兜底
- 当实时数据不可用时，给出说明并降级回答
- 避免虚构高风险信息
- 当预算、时间或路线明显不合理时，主动提示用户

---

## 13. 非功能需求

项目应尽量满足以下工程要求：

- 可扩展：方便增加新工具、新数据源、新 Agent
- 可维护：目录清晰、模块边界明确
- 可测试：支持 mock tools、单元测试、集成测试
- 可运行：最小版本可以本地直接启动
- 可重构：便于后续接入真实 API 和前端系统

---

## 14. 第一版 MVP 范围

为了适合 Claude Code 实战开发，第一版建议聚焦“最小可运行版本”。

### V1 必做
- FastAPI 项目骨架
- `/health` 与 `/chat` 接口
- Planner Agent
- Transport Agent
- Hotel Agent
- Itinerary Agent
- ConversationState 与 TripState
- mock tools
- 基础 RAG 检索
- Agent-Reach 工具适配接口占位
- 方案生成
- 方案调整
- 日志与配置管理
- README 与启动说明

### V1 暂不做
- 前端页面
- 真实第三方平台深度接入
- Agent-Reach 真实渠道的完整配置与自动化调用
- 长期用户偏好记忆
- 复杂推荐排序模型
- 完整权限系统
- 复杂监控与分析后台

---

## 15. 推荐开发顺序

建议按以下顺序推进：

1. 搭建项目骨架
2. 定义 schema 与状态模型
3. 实现 Planner Agent 基础逻辑
4. 实现三个子 Agent 骨架
5. 实现统一 tool interface 与 mock tools
6. 打通 Planner -> 子 Agent -> 汇总输出调用链
7. 增加基础 RAG 检索
8. 增加 Agent-Reach 工具适配接口占位
9. 补充日志、异常处理、README
10. 最后再考虑真实 API / MCP / Agent-Reach 渠道对接

---

## 16. 适配 Claude Code + Superpowers 的开发方式

推荐工作流：

1. 先用 superpowers 做需求梳理与实施计划
2. 再用 subagent-driven-development 分模块执行
3. 每次只推进一个明确模块
4. 先实现 mock 版本，再替换真实能力
5. 先保证能跑通，再逐步重构优化

核心原则：

- 不一次性生成整个项目
- 不过早接入真实第三方服务
- 先定义状态，再定义 Agent，再定义工具，再搭图流程

---

## 17. 总结

该项目的最终目标不是做一个普通聊天机器人，而是做一个具备任务拆解、外部能力调用、多模块协同和结构化状态管理的 AI Agent 应用。

当前架构已经明确为：

- 1 个主 Agent：Planner
- 3 个子 Agent：Transport、Hotel、Itinerary
- 天气、地图、搜索、RAG、Agent-Reach 外部检索等通过 tool / MCP 接入

在工程实施上，应优先完成最小可运行版本，以便后续继续扩展真实数据接入、复杂策略优化与前端展示。

---

## 18. V2 版本规划（2026-04-14）

### 18.1 版本目标
在现有 V1 可运行 MVP 基础上，完成“可用性增强 + 真实能力接入准备 + 可观测性提升”，使系统从“演示可跑”升级到“可持续迭代”。

核心目标：
- 提升多轮对话下的状态稳定性与可解释性
- 引入可切换的真实工具适配层（保留 Mock 兜底）
- 强化行程输出质量（时间冲突、预算冲突、节奏冲突）
- 完成最小可观测能力（日志、请求追踪、错误分类）

### 18.2 V2 范围（In Scope）
- Planner 增强：意图识别、冲突检测、追问策略
- StateManager 增强：状态变更审计、版本化快照、回滚点
- Tools 增强：`AgentReachAdapter` 实现首批只读检索能力（网页/RSS）
- Itinerary 增强：按天块化输出 + 可读时间段模板
- API 增强：新增 `POST /plan/{session_id}/revise` 局部重规划接口
- 工程增强：结构化日志、统一错误码、基础指标埋点

### 18.3 V2 不做（Out of Scope）
- 前端页面与账号体系
- 支付、下单、真实交易闭环
- 复杂推荐排序模型（先规则后模型）
- 长期用户画像系统

### 18.4 文件与职责（V2 新增/调整）
- 新增：`app/services/plan_revision_service.py`：局部重规划编排
- 新增：`app/utils/logging.py`：结构化日志初始化
- 新增：`app/utils/error_codes.py`：统一错误码与映射
- 新增：`app/observability/metrics.py`：最小指标封装
- 修改：`app/api/routes.py`：新增 `/plan/{session_id}/revise`
- 修改：`app/agents/planner.py`：冲突检测与追问策略
- 修改：`app/state/manager.py`：状态快照与审计字段
- 修改：`app/tools/agent_reach_adapter.py`：实现首批检索能力
- 新增：`tests/test_plan_revision_api.py`：重规划接口测试
- 新增：`tests/test_planner_conflict_detection.py`：冲突检测测试
- 新增：`tests/test_state_snapshot.py`：状态快照测试

### 18.5 迭代任务拆分（建议按 TDD 执行）

#### Task A：局部重规划接口
- [ ] 新增失败测试：`/plan/{session_id}/revise` 在修改预算/酒店偏好后返回更新方案
- [ ] 实现最小接口与 service 编排
- [ ] 回归测试：原 `/chat` 与 `/plan/{id}` 不回归
- [ ] 提交：`feat: add plan revision endpoint and service`

#### Task B：Planner 冲突检测与追问
- [ ] 新增失败测试：预算不足/时间冲突场景触发 `pending_questions`
- [ ] 实现冲突检测规则（预算、时长、交通衔接）
- [ ] 输出冲突解释（中文）与追问建议
- [ ] 提交：`feat: add planner conflict detection and follow-up questions`

#### Task C：State 快照与审计
- [ ] 新增失败测试：每次保存方案生成快照版本号
- [ ] 实现 `save_plan` 快照链（latest + history）
- [ ] 支持按 `session_id` 读取最近 N 个版本
- [ ] 提交：`feat: add state snapshot and audit trail`

#### Task D：AgentReach 首批可用接入（只读）
- [ ] 新增失败测试：`AgentReachAdapter` 返回标准化检索结果
- [ ] 实现网页/RSS 检索适配与超时兜底
- [ ] 失败降级为 Mock，不中断主流程
- [ ] 提交：`feat: implement agent-reach read-only adapter with fallback`

#### Task E：可观测性与错误治理
- [ ] 新增失败测试：关键错误返回统一错误码
- [ ] 实现结构化日志（request_id/session_id/intent）
- [ ] 增加基础指标：请求总数、错误数、平均耗时
- [ ] 提交：`chore: add observability and unified error codes`

### 18.6 验收标准（DoD）
- 所有新增测试通过，且全量测试通过
- 新增代码遵循中文注释规范（重点注释关键逻辑）
- `/plan/{id}/revise` 在至少 3 类修改场景下可用（预算、酒店偏好、节奏）
- Planner 对冲突场景能给出明确解释，不返回空泛提示
- AgentReach 失败不影响主链路，系统可稳定降级
- 日志可关联单次请求链路（request_id -> session_id -> plan_id）

### 18.7 里程碑建议
- M1（1 天）：Task A + Task B
- M2（1 天）：Task C
- M3（1 天）：Task D + Task E
- M4（0.5 天）：全链路回归、文档更新、发布检查

### 18.8 风险与应对
- 外部检索不稳定：统一超时 + 熔断 + Mock 降级
- 规则冲突误报：先小规则集，配测试样例逐步扩展
- 状态膨胀：快照数量上限 + 归档策略
- 调试困难：强制关键日志字段与错误码规范

### 18.9 V2 交付物清单
- 可运行后端（新增重规划接口）
- 冲突检测与追问能力
- 状态快照与审计能力
- AgentReach 首批只读接入
- 可观测能力（日志 + 指标 + 错误码）
- 更新后的开发文档与测试报告

---

## 19. V3 版本规划（FastAPI + Redis + MySQL + LangGraph）

### 19.1 版本目标
基于现有 V2 能力，构建“异步任务编排 + 状态可恢复 + 执行结果回填”的 AI Agent 服务闭环，支持多会话并发场景下的稳定运行。

### 19.2 架构升级方向
- API 层：FastAPI 负责接入与任务查询，不阻塞长任务。
- 编排层：LangGraph/StateGraph 驱动主 Agent 与子 Agent 执行流。
- 运行层：Redis 负责任务队列、运行态缓存、幂等与并发控制。
- 持久层：MySQL 负责会话、任务、步骤轨迹、方案快照等权威存储。
- 回填层：每个 Agent 节点执行完成后写入任务步骤表，并更新会话/方案快照。

### 19.3 核心流程闭环
1. 用户调用 `/chat`，系统创建 `task_id` 并写入 MySQL，随后入 Redis 队列。
2. Worker 消费任务并执行 LangGraph 工作流。
3. 节点执行结果持续回填 `task_steps` 与运行态缓存。
4. 全链路完成后生成新 `plan_snapshot` 并更新任务状态。
5. 用户通过 `/task/{task_id}`、`/session/{id}`、`/plan/{id}` 查询进度与结果。
6. Worker 重启后可从 MySQL + Redis checkpoint 恢复未完成任务。

### 19.4 V3 交付项
- 任务状态机：`PENDING -> RUNNING -> WAITING_INPUT -> SUCCEEDED/FAILED/CANCELED`
- 新增任务查询接口与方案历史接口
- MySQL 会话与任务持久化
- Redis 队列消费与恢复机制
- Agent 节点结果回填与可观测追踪（request_id/task_id/session_id）

### 19.5 风险与控制
- 外部工具不稳定：超时、重试、降级、死信策略
- 会话并发写冲突：`session_id` 粒度锁与幂等键
- 恢复一致性问题：写库优先 + checkpoint 对账策略

### 19.6 下一步实施文档
详见：`docs/superpowers/plans/2026-04-14-v3-fastapi-redis-mysql-agent-loop.md`

---

## 20. V3 实施进展（2026-04-15）

### 20.1 本轮已完成
- 异步任务入口：`POST /chat/async` 创建任务并入队。
- 任务查询接口：`GET /task/{task_id}`、`GET /task/{task_id}/steps`。
- 方案历史接口：`GET /plan/{session_id}/history`（优先内存历史，回退 MySQL 快照）。
- 队列消费能力：
  - 单次消费：`POST /tasks/consume-once`
  - 批量消费：`POST /tasks/consume-batch`
  - 恢复执行：`POST /tasks/recover`
- Worker 能力补齐：支持 `run_loop` 常驻消费、`stop` 优雅停止、`resume_incomplete_tasks` 恢复未完成任务。
- 回填链路：节点执行结果写入 `task_steps`，成功后落 `plan_snapshots`，并更新 `tasks.status`。

### 20.2 测试与验证
- 当前自动化测试：`31 passed`。
- 覆盖新增能力：
  - 多任务批量消费（多会话场景）
  - Worker 循环消费与停止控制
  - 任务恢复与步骤回填

### 20.3 当前可直接演示的闭环
1. 调用 `/chat/async` 提交任务。
2. 调用 `/tasks/consume-batch` 或 `/tasks/consume-once` 执行任务。
3. 调用 `/task/{task_id}` 查看状态。
4. 调用 `/task/{task_id}/steps` 查看回填步骤。
5. 调用 `/plan/{session_id}/history` 查看方案历史。

### 20.4 待生产化对接（不影响本地最小闭环）
- `.env` 中切换真实 `REDIS_URL` 与 MySQL `DATABASE_URL`。
- 结合部署方式将 `task_worker.run_loop` 作为独立后台进程常驻运行。
- 增加死信队列、重试上限与告警策略。

### 20.5 本轮补齐的“未完成功能”
- 任务状态机补齐：支持 `WAITING_INPUT`、`CANCELED` 状态。
- 状态迁移约束：仓储层新增 `try_transition_task_status`，可限制合法来源状态，避免非法状态回退。
- 取消任务接口：新增 `POST /task/{task_id}/cancel`。
- Worker 幂等保护：若任务已终态（`SUCCEEDED/FAILED/CANCELED`）则跳过执行，避免重复消费覆盖结果。
- 人工补充状态：当编排返回 `needs_user_input=True` 时，任务进入 `WAITING_INPUT`。

### 20.6 新增测试覆盖
- `tests/test_persistence_repository.py`
  - `test_try_transition_task_status_respects_allowed_from`
- `tests/test_task_api.py`
  - `test_task_cancel_endpoint_changes_status`
- `tests/test_worker_recovery.py`
  - `test_worker_sets_waiting_input_when_runner_requests_input`
  - `test_worker_skips_canceled_task`

### 20.7 最新验证结果
- 全量测试：`35 passed`（`conda run -n leetcode pytest -v -p no:cacheprovider`）。

### 20.8 最终完成状态（2026-04-15）
- V3 任务状态机闭环已完成：`PENDING -> RUNNING -> WAITING_INPUT -> SUCCEEDED/FAILED/CANCELED`，并包含 `RETRYING` 与重试上限策略。
- Redis/本地队列能力已补齐：幂等键（TTL）、session 粒度锁、DLQ、坏 payload 降级处理。
- 恢复策略已补齐：基于 stale 判定恢复 `RUNNING/RETRYING`，并支持超时 `PENDING(payload_json)` 恢复，降低“出队后崩溃”丢任务风险。
- StateGraph 执行已升级为节点化流程（`ingest/planner/finalize`），支持 checkpoint 输入输出、节点跳过与节点级回填。
- Worker 已支持 checkpoint 对账（队列 payload 与持久化 payload 合并）与恢复续跑，减少重复回填。
- 可观测链路已补齐：任务创建到执行日志可串联 `request_id/task_id/session_id/plan_id`。
- 独立 Worker 常驻入口已提供：`python -m app.workers.run_worker`。
- 关键接口异常语义已修正：缺失 task 返回 404，`/tasks/consume-once` 返回 `consumed`（是否出队）与 `processed`（是否处理成功）。

### 20.9 最新自动化验证
- 全量测试（leetcode 环境 Python 3.10）：`72 passed`。
- 命令：`conda run -n leetcode pytest -q -p no:cacheprovider`。

### 前端与账号体系实施进展

- 已规划并实现普通用户聊天应用风格 Web 前端。
- 第一版包含登录、注册、聊天首页、历史侧栏、方案详情抽屉。
- 后端账号体系包含用户注册、登录、当前用户和用户级资源隔离。
- 前端位于 `frontend/`，通过 `VITE_API_BASE_URL` 连接 FastAPI 后端。

### 单一配置文件实施进展

- 项目运行时统一使用 `config.yaml` 作为唯一配置文件。
- 仓库提供 `config.example.yaml` 作为模板，不提交真实 cookie、API key 或密码。
- `.env.example` 已废弃，LLM、数据库、Redis、真实平台 provider、Agent-Reach 渠道配置统一进入 YAML。
- 默认配置已切换为 `use_mock_only=false`、`enable_agent_reach=true`、`enable_real_providers=true`，真实平台缺少 cookie/key 时后续 adapter 应降级处理。
- 已预留携程、飞猪、美团、高德地图、天气服务以及小红书、抖音、微博、B站、YouTube 等 Agent-Reach 渠道配置槽位。
